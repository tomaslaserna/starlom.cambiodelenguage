# Handoff — Sesión 2026-07-01 (continuar mañana)

## Dónde estás parado

Proyecto: `starlom.cambiodelenguage` (ERP Next.js 16 en `apps/web`), rama **`main`**.
Base de datos: Supabase **nuevo** `tkpapbinlplqfhoxesuk` (sa-east-1), esquema en
inglés. `apps/web/.env.local` ya está configurado (gitignored). Login de prueba:
`ftrdistribuciones@gmail.com` / `Starlim2026!` (admin). Levantar la app:
`cd apps/web && npm run dev` → http://localhost:3000. Validar DB:
`/api/health` debe dar `database.ok=true`.

**Git:** `main` local está **10 commits adelante de `origin/main`, SIN pushear**
(así lo quiso el usuario). No pushear al repo compartido de Tomás sin OK explícito.

## Lo que se construyó hoy (todo commiteado en main local)

1. Navegación: sección **Inicio** (Calendario/Mensajes) y sección **Comercial**
   (Pedidos/Ventas/Presupuestos). Se quitó el link "Inicio" redundante.
2. **Generador de confirmación WhatsApp** en "Cargar pedido" (`/orders/new`):
   `lib/order-confirmation.ts` (función pura + tests) + `order-confirmation-preview.tsx`.
3. **Editar pedidos confirmados**: se relajó el candado en `updateBasicOrder`; al
   guardar, el pedido vuelve a `cargado`.
4. Se verificó que el **registro de pedidos unificado ya existía** (no se rehízo).

## TAREA EN CURSO: Ofertas (Fase A)

**Estado: spec APROBADO por el usuario y commiteado. Falta escribir el PLAN y ejecutar.**

Spec: `docs/superpowers/specs/2026-07-01-offers-phase-a-design.md` (leerlo completo).

Resumen de lo aprobado:
- Migración `033_create_offers.sql`: tabla `offers` (id uuid, title, description,
  active, product_id opcional → products, created_by, timestamps, empresa_id +
  índice `(empresa_id, active)`). Aditiva, bajo riesgo. **Aplicarla a la base
  Supabase con las credenciales de `.env.local`, con OK del usuario.**
- `lib/offers.ts`: listOffers, listActiveOffers, createOffer, updateOffer,
  setOfferActive, offerInputFromBody, tipos Offer/OfferInput.
- ABM en `/pricing/offers` (subpágina de Precios) + `actions.ts`.
- Link "Ofertas" en el header de `/pricing` (NO ítem nuevo en "Base de datos").
- Integración: selector de ofertas activas en `order-confirmation-preview.tsx`
  (al elegir, llena el campo 💡 editable). `order-entry-fields.tsx` y
  `orders/new/page.tsx` pasan las ofertas activas.
- Oferta descriptiva: "vigente" = `active=true` (sin fechas). Sin cálculo de
  descuentos (eso es Fase B).

### PRÓXIMO PASO EXACTO
Invocar la skill **superpowers:writing-plans** para escribir el plan de
implementación de Ofertas Fase A (guardar en `docs/superpowers/plans/`). Luego
ofrecer ejecución (subagent-driven recomendado, o inline) y ejecutar. Antes de
aplicar la migración 033 a la base, pedir OK al usuario.

## Backlog Comercial (después de Ofertas)

- **Fase B: Punto de equilibrio + gating de ofertas** (motor break-even; requiere
  definir con el usuario qué costos fijos/variables, período y fórmula; usa
  `costos_operativos` que ya existe). Diseño propio.
- **Circuito negro/blanco** al confirmar: remito interno (IVA 10,5%) vs factura
  (21%); el más complejo (fiscal). Ya existe base: `sales_internal_documents`,
  `receipt-types.ts`, `billing_document`.
- **Presupuesto rápido / formal**: rápido = mensaje WhatsApp corto; formal = PDF
  con vigencia 7/15 días + registro. `/quotes` y PDFs de quotes ya existen.

## Método de trabajo (respetar)

Cada feature: brainstorming → spec (con aprobación) → writing-plans → ejecución
(subagentes con review por tarea + review global, o inline en cambios chicos) →
finishing. Tests del proyecto: `cd apps/web && node --test scripts/order-confirmation.test.mjs`.
Lint: `npm run lint`. Verificación de lógica que toca la base: flujo real
autenticado (login + curl con cookie), porque no hay harness de tests de DB/UI.

## Notas / riesgos

- Facturación ARCA en `.env.local` está en `production` con certificados que
  apuntan a la máquina de Tomás (no existen en otras) → no emitir facturas reales
  desde local.
- Recordá recargar la app tras cambios de nav para verlos.
